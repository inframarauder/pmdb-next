import React from "react";
import { Layout } from "../../components/common";

const AddTitle = () => {
	return (
		<Layout admin={true}>
			<p>Add Title</p>
		</Layout>
	);
};

export default AddTitle;
